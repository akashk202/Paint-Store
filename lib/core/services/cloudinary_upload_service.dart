import 'dart:io';

class CloudinaryUploadService {
  static Future<String> uploadRaw(File file, {required String folder}) async {
    return ""; // Mocked fallback
  }
  static Future<String> uploadImage(File file, {required String folder}) async {
    return ""; // Mocked fallback
  }
}
